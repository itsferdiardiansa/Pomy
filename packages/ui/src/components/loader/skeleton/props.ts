export type Styles = {
  width: number | string
  height: number | string
  borderRadius: number | string
}
export type SkeletonRectTypes = {
  width: string | number
  height: string | number
  rounded: boolean
}

const skeletonRectProps = {
  width: null,
  height: null,
  rounded: true,
}

export default skeletonRectProps
