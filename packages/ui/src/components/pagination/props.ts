export type PaginationTypes = {
  currentPage: number
  totalPage: number
  perPage: number
  lastPage: number
}

const paginationProps = {
  currentPage: 1,
  totalPage: 0,
  perPage: 10,
  lastPage: 0,
}

export default paginationProps
